$A = {a_1, a_2, ..., a_n}$ multimea de numere date.

=== 1. Spatiul de cautare ($S$)

$ S = { (x_1, x_2, ..., x_n) | (x_1, x_2, ..., x_n) "este o permutare a lui" A } $

=== 2. Setul de candidati ($C$)
$X_k = (x_1, x_2, ..., x_k)$, unde $1 <= k <= n$, $forall i,j in N, i!=j -> x_i!=x_j $ si sa respecte structura de munte.


=== 3. Setul de soluții ($X$)
$ X = { (x_1, x_2, ..., x_n) in X_n } $
